pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage ("Docker") {
            steps {
                dockerfile {
                    dir Docker
                    filename 'Dockerfile'
                    label 'label-docker'
                }
            }
        }
    }
}